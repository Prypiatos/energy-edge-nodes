This directory will hold firmware tests as modules become implementation-ready.

Current validation path:

- `./scripts/validate-config-example.sh`
- `pio run`
- `pio test`

Recommended near-term coverage:

- config loading and persistence
- shared-state/status derivation
- buffer enqueue/flush behavior
- payload formatting for telemetry, health, events, and status
